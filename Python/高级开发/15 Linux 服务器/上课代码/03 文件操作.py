"""
    cat  快速把一个文件的内容显示到命令行
    echo 把内容输出到命令行

    >   重定向,将之前的结果重定向到另外一个地方 (w 覆盖写入)
    >>  追加到文件末尾

    |  管道符号(竖线) 将前一个指令的结果丢给下一个指令进行操作
    grep  正则表达式的匹配指令

    shift + \ = |

    name 是一个编辑器

    ^  ctrl 键
"""
